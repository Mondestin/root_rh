<form id="form{{company.id}}" class="display-contents" method="post" action="{{ path('app_company_delete', {'id': company.id}) }}">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
	<a data-id="form{{company.id}}" class="delete-data btn btn-danger btn-sm delete-btn" title="Supprimer">
		<i class="fas fa-trash"></i>
        Supprimer
    </a>
</form>
