<form id="form{{customer.id}}" class="display-contents" method="post" action="{{ path('app_customers_delete', {'id': customer.id}) }}">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
	<a data-id="form{{customer.id}}" class="delete-data btn btn-danger btn-sm delete-btn" title="Supprimer">
		<i class="fas fa-trash"></i>
        Supprimer
    </a>
</form>
